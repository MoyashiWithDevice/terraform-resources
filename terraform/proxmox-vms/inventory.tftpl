[proxmox_vms]
%{ for vm in vms ~}
%{ set ip = try(flatten(vm.ipv4_addresses)[0], "") ~}
%{ if ip != "" ~}
${vm.name} ansible_host=${ip} ansible_user=bababa
%{ endif ~}
%{ endfor ~}

[all:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
